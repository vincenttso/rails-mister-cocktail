<h1>New Dose</h1>

<div class="container">
  <h1>Add a new dose</h1>
  <%= link_to 'Back to ' + @cocktail.name, cocktail_path(@cocktail), class: 'button-edit' %>
  <div class="form">
    <%= render 'form' %>
  </div>
</div>
